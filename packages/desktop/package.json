{
  "name": "@pi-deck/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint .",
    "test": "bun test",
    "dist": "bun scripts/dist-stub.ts"
  },
  "dependencies": {
    "@pi-deck/core": "workspace:*",
    "@pi-deck/ui": "workspace:*"
  },
  "devDependencies": {
    "electron": "^42.0.1"
  }
}
