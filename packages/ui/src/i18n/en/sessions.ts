/** Strings for `features/sessions/`. Phase 07 fills in the rest of the surface. */
const sessions = {
  filter: {
    /** Project-filter summary in the sessions toolbar. */
    selectedCount: "{count:number} selected",
  },
} as const;

export default sessions;
